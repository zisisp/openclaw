# Task List

1. **Handle Zina's URL failure** — investigate and fix the failing URL.

2. **Provide the mock server and mock URLs** — set up a mock server and expose mock URLs.

3. **Handle the Klidarithmos activation process** — call the activation endpoint and then connect with LDAP.

4. **Create a new submission and declaration ID** — an ID generated when a form is submitted in ISS, or when a form is completed in IRM.

5. **Reply with the FMI submission error codes** — send the error codes for FMI submission in reply to the email Lena sent.
